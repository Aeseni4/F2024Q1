const carsArray = ['Jaguar', 'McLaren', 'audit', 'BMW', 'toyota' ]

for(let y=0; y < carsArray.length; y++){
    if(carsArray[y]==='toyota'){
        console.log(`в данном массиве нйден ${carsArray[y]}`)
    }
}


carsArray[0]

